# App Server generated JSON Schemas

Run the following command in an environment where `codex` CLI is available:

```bash
codex app-server generate-json-schema --out ./schemas
```

Generated artifacts from Codex App Server should be committed under this directory.
