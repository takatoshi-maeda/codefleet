# App Server generated types

Run the following command in an environment where `codex` CLI is available:

```bash
codex app-server generate-ts --out ./src/generated/app-server/types
```

This repository keeps the generated output under this directory.
